{
  "name": "mhgl",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev-go.sh",
    "build": "go build -o .build/mhgl ./cmd/server",
    "start": ".build/mhgl",
    "lint": "go vet ./..."
  }
}
