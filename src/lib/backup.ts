// ============================================================
// 数据备份 — 前后端共享常量(单一真值源)
// ============================================================

// [R21-g-2] 大书库降级阈值: books > 该值时备份仅导出书籍元数据(不含章节正文)。
// 消费方: 后端 src/app/api/admin/backup/route.ts(导出降级) +
//        前端 src/components/admin/BackupSection.tsx(「大库模式」徽标 + 「大库降级」文案)。
// 两端必须同值 —— 修前前端硬编码 500 而后端 R4A-12 已降至 200, 徽标/文案与实际
// 行为漂移(books=300 时后端已元数据化导出, 前端仍显示全量文案且不亮大库徽标)。
// R4A-12 依据: 499 书 × 500 章节场景 dumpBooksFull 单 findMany 全量行入内存,
// JSON.stringify payload 多百 MB 堆峰值; 200 上限下站群常见 100~200 书仍导出含章节。
export const BACKUP_BIG_BOOKS_THRESHOLD = 200
